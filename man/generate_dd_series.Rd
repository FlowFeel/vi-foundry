% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/speculative.R
\name{generate_dd_series}
\alias{generate_dd_series}
\title{Generate a diversity time series with parameterized feedback (internal)}
\usage{
generate_dd_series(n_steps, innovation_rate, capacity, feedback, seed = 42L)
}
\arguments{
\item{n_steps}{Integer. Time series length.}

\item{innovation_rate}{Numeric. Base rate r.}

\item{capacity}{Numeric. Carrying capacity K.}

\item{feedback}{Numeric in \link{0, 1}. 0 = logistic (negative DD), 1 =
autocatalytic (positive DD). Bifurcation at 2/3.}

\item{seed}{Integer. RNG seed (unused — deterministic, A2 — but kept for
contract consistency).}
}
\value{
Numeric vector. Innovation counts over time.
}
\description{
Generates an innovation-count time series whose per-capita rate blends
autocatalytic (positive DD) and bounded-logistic (negative DD) growth:
pc_rate = r * \link{feedback * (0.5 + 0.5*N/(N+K)) + (1-feedback) * K/(N+K)}
At feedback=0: bounded logistic (negative DD, niche-filling).
At feedback=1: bounded autocatalytic (positive DD, Homo inversion) —
matches generate_autocatalytic_set() exactly.
Bifurcation at feedback = 2/3 (DD slope = 0).
}
\keyword{internal}
